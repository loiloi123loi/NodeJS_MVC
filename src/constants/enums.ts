export enum ToastType {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
  WARNING = 'warning'
}

export enum LoginProvider {
  LOCAL
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER'
}
